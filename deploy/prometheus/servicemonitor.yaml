apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: shree
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: shree
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
